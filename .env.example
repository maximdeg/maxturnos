# =============================================================================
# MaxTurnos — Environment variables (copy to .env.local and fill values)
# Production: set NODE_ENV=production, TEST_MODE=false, and all required vars.
# =============================================================================

# --- Required for production ---
JWT_SECRET=                          # Min 32 chars. Used for provider JWT and cancellation tokens.
POSTGRESQL_HOST=
POSTGRESQL_DATABASE=
POSTGRESQL_USER=
POSTGRESQL_PASSWORD=
POSTGRESQL_PORT=5432
NEXT_PUBLIC_APP_URL=                 # Public app URL (e.g. https://maxturnos.example.com)

# --- Database SSL (recommended in production) ---
# POSTGRESQL_SSL_MODE=require
# POSTGRESQL_CA_CERT=                # Optional: RDS/Cloud SQL CA cert PEM

# --- Rate limiting (required in production for protection) ---
# If unset, rate limits are disabled (allow all). Set for production.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# --- Email (for verification and notifications) ---
EMAIL_USER=
EMAIL_PASS=

# --- Optional / defaults ---
AUTH_SECRET=                         # NextAuth / app auth (optional)
LOG_LEVEL=info                       # debug | info | warn | error. Use info/warn in production.
# TEST_MODE=                         # Must be unset or false in production.

# --- Demo provider (optional, for landing) ---
# NEXT_PUBLIC_DEMO_PROVIDER_USERNAME=demo

# --- Super admin (scripts: create-super-admin, change-super-admin-password) ---
# SUPER_ADMIN_EMAIL=
# SUPER_ADMIN_PASSWORD=

# --- Backend URL fallback (optional) ---
# BACKEND_API_PROD=

# --- Cron: recordatorios 30h antes (cron externo) ---
# CRON_SECRET=              # Secreto para autorizar GET/POST /api/cron/send-reminders (header Authorization: Bearer <secret>)

# --- WhatsApp (optional) ---
# UltraMsg
# ULTRAMSG_API_URL=https://api.ultramsg.com
# ULTRAMSG_INSTANCE_ID=
# ULTRAMSG_API_TOKEN=
# ULTRAMSG_URL=
# WHAPI
# WHAPI_API_TOKEN=
# WHAPI_PHONE_NUMBER_ID=
# WHAPI_API_URL=https://gate.whapi.cloud/
# Meta / legacy
# WHATSAPP_TOKEN_API=
# WHATSAPP_ID_PHONE_NUMBER=
# WHATSAPP_ID_BUSSINESS_PHONE_NUMBER=

# --- Other optional ---
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=
# BASE_URL=                          # For scripts (default localhost:3000)
